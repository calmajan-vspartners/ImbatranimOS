{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": [
        "$TURBO_DEFAULT$",
        "../add-ons/*/src/**",
        "../../packages/*/src/**"
      ]
    },
    "typecheck": {
      "inputs": [
        "$TURBO_DEFAULT$",
        "../add-ons/*/src/**",
        "../../packages/*/src/**"
      ]
    },
    "test": {
      "inputs": [
        "$TURBO_DEFAULT$",
        "../add-ons/*/src/**",
        "../../packages/*/src/**"
      ]
    }
  }
}
