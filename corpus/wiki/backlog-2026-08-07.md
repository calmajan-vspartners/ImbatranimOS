---
summary: The 2026-08-07 research-sweep backlog — briefs 101-136, one line each, from five parallel code-exploration lenses (deferral harvest, shell UX, app depth, platform resilience, fit-and-finish). Ordered in build waves; each row names its evidence class. All ungrilled proposals; grill before building.
updated: 2026-08-07
---

# Backlog — the 2026-08-07 research sweep

Written after briefs 47-51 + 80-100 closed the previous backlogs (the seam is
live in all 26 apps, dotfiles/associations/startup/workspaces shipped). Five
parallel read-only exploration lenses over the code and corpus produced ~30
evidenced candidates; this page is the synthesis. **All are ungrilled** —
researched proposals, not locked decisions. Grill against the code before
building.

Brief files exist for 101-113 (authored and code-re-verified the same day);
rows 114-136 get their brief file written at grill time, when each is reached —
the row below is the synthesis to grill from.

Recurring sources worth naming: the corpus's own recorded deferrals (16 done
briefs carry them), the user-filed `desktop-drag-selection` todo, the
promote-on-third-use rules that have already fired (context menu, six-hue
palette), and holes the brief-48 seam either fixed the plumbing for
(`onIntent` has zero consumers) or made cheap (protocol additions are one
file).

## Wave A — platform trust + shell feel (101-110)

| # | brief | size | one line |
|---|---|---|---|
| 101 | [lock-keeps-the-desktop](../briefs/done/101-lock-keeps-the-desktop.md) | MED | **DONE 2026-08-07** — lock/401 is an opaque overlay over a hidden+inert desktop (PTYs and dirty buffers survive; probe: same shell ticked through lock, "Session ended" honestly reported after revoke); sliding expiry via guard-only `renewIfDue` + cookie re-issue, capped 30 days; log off stays the full teardown |
| 102 | [async-close-guards-themed-confirm](../briefs/done/102-async-close-guards-themed-confirm.md) | MED | **DONE 2026-08-07** — protocol v2: async close guards; kit `UnsavedChangesDialog` with the real-OS third button (Save and close, trusted via the dirty flag); all 9 editors + image-viewer's sibling confirm migrated; `window.confirm` greps to zero (probe: terminal streamed 10→13 ticks behind the open dialog) |
| 103 | [window-management-parity-2](../briefs/done/103-window-management-parity-2.md) | EASY | **DONE 2026-08-07** — viewport resize reflows everything (probe: maximized 1400×856→1000×656 exactly, snapped halves retile); double-click-titlebar maximize; `mod+alt+shift+arrows` snap sequence; `ctrl+alt+d` show desktop; `ctrl+alt+(shift+)1..4` jump/carry via the new `e.code` digit matcher; taskbar menu Maximize row |
| 104 | [alt-tab-switcher](../briefs/todo/104-alt-tab-switcher.md) | MED | Alt+Tab is blind roulette: no overlay, no MRU order, no reverse cycling. The visual switcher every desktop has |
| 105 | [context-menu-kit](../briefs/todo/105-context-menu-kit.md) | MED | The third hand-rolled context menu shipped (file-manager, taskbar, desktop) and they already disagree — file-manager's clips off-screen and none has keyboard nav. One kit menu with the ARIA contract; migrate all three |
| 106 | [desktop-interaction-pack](../briefs/todo/106-desktop-interaction-pack.md) | MED | Right-click an icon opens the *widgets* menu (dead attribute guard — verified), and with no widgets the background right-click is a silent no-op; the user-filed drag-selection marquee todo. Icon menus, an always-alive background menu, rubber-band multi-select |
| 107 | [actionable-notifications](../briefs/todo/107-actionable-notifications.md) | MED | Toasts are dead ends: no click-to-open (the panel has it — proven parity gap), no actions. Protocol gains `actions` as intent-shaped data so brief 93's alarm toasts carry Snooze and destructive flows can carry Undo |
| 108 | [intent-redelivery](../briefs/todo/108-intent-redelivery.md) | EASY | Open zip A, double-click zip B: Archive Manager focuses still showing A — the payload is silently dropped (ref-guarded consume-once; `onIntent` exists with zero consumers). The dead double-click, back through the side door |
| 109 | [dotfile-write-durability](../briefs/todo/109-dotfile-write-durability.md) | EASY | A failed prefs PUT is cleared from the queue *before* the request and swallowed — the change looks applied for days, then snaps back at next sign-in. Retry, keepalive flush on pagehide, re-flush after re-auth |
| 110 | [migration-ledger](../briefs/todo/110-migration-ledger.md) | EASY | `migrate()`'s six bare `try/catch` ALTERs swallow SQLITE_FULL/READONLY the same as duplicate-column — a disk-full boot leaves a half-migrated schema and cryptic 500s. PRAGMA user_version ledger; each step runs once; real failures surface |

## Wave B — app depth, small and ripe (111-123)

| # | brief | size | one line |
|---|---|---|---|
| 111 | [file-manager-keyboard-parity](../briefs/todo/111-file-manager-keyboard-parity.md) | MED | The flagship app's every verb is right-click-only: F2, Delete/Shift+Delete, Ctrl+C/X/V/A, Shift-range select, the menu key, and the mouse-only splitter (markdown-editor's SplitDivider is the house-correct twin) |
| 112 | [file-manager-search](../briefs/todo/112-file-manager-search.md) | EASY | The jailed, bounded name+content search endpoint has existed since brief 45 — the file manager just never grew a search box. Scoped-to-here search with honest truncation |
| 113 | [code-editor-find-in-files](../briefs/todo/113-code-editor-find-in-files.md) | MED | The brief-88 deferral: Ctrl+Shift+F over the same bounded grep, results panel, click lands at the line |
| 114 | git-diff-compare-with-head | MED | The brief-99 deferral: `git show` joins the allowlist with brief-76-grade argument scrutiny; Git GUI's file rows gain Compare with HEAD in the Monaco diff |
| 115 | terminal-unicode-width | EASY | Emoji and CJK filenames corrupt the terminal grid — cell-width is wrong without the xterm unicode addon (same sanctioned family as the three addons already shipped) |
| 116 | calculator-unit-converter | EASY | The fourth mode tab every OS calculator ships: length/mass/temperature/data, pure static tables over the existing evaluator + tape |
| 117 | todo-move-between-lists | EASY | Refiling a task is delete-and-retype today, losing its due date and priority — the update mutation already accepts the list id; only the UI is missing |
| 118 | solitaire-free-drag | MED | The brief-98 deferral: Klondike without card dragging reads as broken next to the Win7 original the identity invokes; click-to-place stays as the accessible path |
| 119 | markdown-links-through-associations | EASY | A README's link to an image/CSV/PDF shows a toast telling you to go find it — route non-markdown relative links through the brief-81 resolver instead |
| 120 | media-player-playlists | MED | Queues are folder-only and die with the window. M3U files in the home FS (the ICS/Netscape-HTML interop precedent), `.m3u` in the app's `opens` |
| 121 | code-editor-folder-tree | MED | Open Folder: an explorer sidebar over the existing list route + `pickDirectory`, virtualised with the house hook — the largest remaining distance from the editor the app imitates |
| 122 | rest-client-sse | MED | The API tool cannot watch a stream — SSE/chunked pass-through on the existing proxy pipeline, token-by-token response pane (WebSocket stays deferred) |
| 123 | norpdf-first-paint | MED | The recorded ~1.2 s cold-open regression vs the light viewer: paint page one before the thumbnail rail warms — every PDF double-click in the OS pays this |

## Wave C — bigger, cross-app, or grill-first (124-136)

| # | brief | size | one line |
|---|---|---|---|
| 124 | totp-recovery-codes | MED | Promotes the standing todo: lose the phone today and the machine is gone. Hashed one-time codes, show-once, step-up + throttle + audit integration; strictly a second factor, never password reset |
| 125 | active-sessions-revoke | MED | Brief 84 shows past sign-ins; nothing answers "what is signed in NOW". Sessions list with IP/UA/this-device badge, per-session revoke, sign-out-everywhere |
| 126 | scheduled-backups | MED | The Tier-2 entry whose own text gated it on brief 80 (shipped): nightly snapshot into the home volume, N-generation retention, free-space floor, "protects against mistakes, not disasters" honesty in the UI |
| 127 | todos-on-calendar | MED | The thrice-deferred coupling: due todos render read-only on the month grid, Agenda and the desktop widget; click-through opens Todo. Shared storage, never app-to-app IPC |
| 128 | export-to-pdf | MED | Deferred by three briefs to exactly this shape: one platform print path (print iframe + print CSS → the browser's Save-as-PDF), Markdown + Notepad first, Docs behind a fidelity spike |
| 129 | kit-consolidation-2 | MED | Tabs (eight hand-rolled strips, one with ARIA) and EmptyState (five copies) into the kit; the six-hue palette's promote-on-third rule already fired (Paint hardcodes the same hexes) |
| 130 | start-menu-filter-and-pins | EASY | Type-to-filter in the Start menu and a pinned row — stored as a dotfile, registered in BOTH DOTFILE_KEYS and rehydrateDotfileStores (the trap briefs 81 and 82 each hit) |
| 131 | palette-system-verbs | EASY | The palette can only open things — lock, DND, workspace N, log off, straight-to-Settings-section become typeable verbs via the existing CommandSource seam |
| 132 | drop-to-upload | EASY | A mis-aimed OS-level drop navigates the tab away and kills the session (no global guard — verified); catch it, and make desktop-background drop upload to home via the existing authed route |
| 133 | file-manager-drag-move | HARD | The brief-55 deferral: drag a file onto a folder/tree node to move it — sequenced after 106 so the OS grows one drag model, not three |
| 134 | desktop-files-layer | HARD | ~/Desktop becomes a real folder rendered as icons beside app shortcuts — files dropped or saved there are visibly on the desktop and survive reboot; opens via the brief-81 resolver |
| 135 | live-folders-fs-watch | HARD | `touch` in the Terminal is invisible to the Files window beside it. A new `system.fs.watch` capability + an authed watch gateway on the PTY's upgrade pattern; refcounted non-recursive inotify on displayed dirs only |
| 136 | git-sync-credentials | HARD | Push/pull/fetch — the half of a git client daily use touches most. GRILL FIRST: the credential story (HTTPS PATs only, encrypted at rest, never logged) is a decisions.md entry before any code |

## Not promoted, with the reason

- **Anything on real-os-gaps.md's rejection list** — unchanged, not re-litigated.
- **Sound recorder** (Tier 2) — needs mic permission + secure context; real value
  is thin against the roster and the HTTPS story belongs to brief 15's deploy.
- **Keyboard shortcut rebinding** — real, but a settings surface with heavy
  conflict-handling design; parked until the parity packs (103/104) settle what
  the default map even is.
- **Hex viewer** (Tier 2) — still fits; parked behind Wave B's higher-value app
  work. Candidate for a future sweep.
- **Session reattach / tmux-style persistence** — still rejected; 101's overlay
  lock deliberately keeps the PTY alive *client-side* without any server session
  state, which is the honest version of the same want.
