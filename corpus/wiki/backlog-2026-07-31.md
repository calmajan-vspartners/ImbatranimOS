---
summary: The 2026-07-31 improvement sweep backlog — briefs 52-86, one line each. 52-54 cross-cutting platform fixes, 55-78 one per app (all 24 registry apps), 79-86 real-OS parity. All ungrilled proposals from a research sweep; grill before building. Includes the dependency order and what was already fixed in-session.
updated: 2026-07-31
---

# Backlog — the 2026-07-31 improvement sweep

Briefs 52-86, written from a live walkthrough of the running OS plus a code
read. **All are ungrilled** — they are researched proposals, not locked
decisions, and each says so in its header. Grill before building.

Split out of [status.md](status.md), which has a 200-line cap.

## Already fixed in-session (not briefs)

Four production bugs found during the sweep were fixed and merged the same day
— see [log.md](../log.md) 2026-07-31:

- **Notepad's notes never survived recreating the container.** The notes root
  was hardcoded to `<cwd>/data/notes` and ignored `NOTES_DIR`, so it resolved
  inside the image's writable layer while the volume sat elsewhere. Silent data
  loss, against the README's explicit promise. The only one of the four that
  destroyed user data.

- **System Monitor's process table was empty in every shipped image.** The
  backend shelled out to procps `ps` syntax; Alpine has busybox. Replaced with a
  `/proc` walk.
- **The Git app was dead in the container and on the ISO** — `git` was installed
  in neither. Installed in both, plus a clear 503 if it is ever missing again.
- **Core's `Tooltip` emitted `<button>` inside `<button>`** across 33 call sites.
- Plus: the repo could not `npm install` with its own declared npm 11, and
  `format:check` was red in two packages.

## Cross-cutting platform (land first)

| # | Brief | Size | Why first |
|---|---|---|---|
| 52 | [window-default-size-clamp](../briefs/done/52-window-default-size-clamp.md) | EASY | **DONE 2026-07-31** — windows no longer render past the taskbar |
| 53 | [desktop-icon-layout](../briefs/done/53-desktop-icon-layout.md) | EASY/MED | **DONE 2026-07-31** — icons reflow by viewport; no overlaps |
| 54 | [shared-file-open-dialog](../briefs/done/54-shared-file-open-dialog.md) | MEDIUM | **DONE 2026-07-31** — `useFileDialog` + all 8 apps wired |

## One per app (55-78)

| # | App | Headline |
|---|---|---|
| 55 | File Manager | **DONE 2026-08-04** — [brief](../briefs/done/55-file-manager-explorer-parity.md). Sorting (3 keys, both ways, `aria-sort`), Icons view, Ctrl+H hidden toggle, one dialog dialect. Properties was **already done** in brief 87 and the list already had Size/Modified columns. Also fixed: `FileList` was handed the **unsorted** array while the virtualizer counted the sorted one |
| 56 | Terminal | **DONE 2026-08-04** — [brief](../briefs/done/56-terminal-reconnect-and-affordances.md). Reconnect verified against a **real `kill -9` + restart**, scrollback intact. Search, web-links, font size, and the xterm theme now from tokens (light mode needs its **own ANSI 16** — xterm's defaults are unreadable on white). Found: my own Ctrl+Shift+V handler made paste arrive **twice**; xterm already handles the native paste event |
| 57 | Settings | **DONE 2026-08-04** — [brief](../briefs/done/57-settings-password-and-real-about.md). `POST /auth/password`: re-proves the current password, TOTP step-up, throttled on login's counter, **every** session evicted and the caller re-issued. About now reads `/api/system/about`; the footer follows `IMAGE_VERSION` instead of claiming `v0.1`. Verified across **two browser contexts** |
| 58 | System Monitor | **DONE 2026-08-04** — [brief](../briefs/done/58-system-monitor-history-and-safety.md). Kill now confirms, and killing the **backend's own pid** gets its own warning. Sparklines from a client ring buffer (no chart dep), loadavg/per-core/swap/uptime/network added. Also fixed: **the backend had no `typecheck` script**, so `src/` was only ever type-checked by `nest build` |
| 59 | Notepad | **DONE 2026-08-04** — [brief](../briefs/done/59-notepad-one-filesystem.md). Defaults to `home`, `notes` still reachable. Find/replace, status bar, size guard → Code Editor, private FileBrowser deleted. The brief called autosave **"rejected"** but Notepad *was* the autosaving app — converted to the real save spine. Also fixed: a `.txt` in `home` opened **nothing at all** |
| 60 | Markdown Editor | **DONE 2026-08-05** — [brief](../briefs/done/60-markdown-editor-authoring.md). Formatting toolbar + focus-scoped keymap (undo-preserving, via `execCommand`), scroll sync on **every** block (measured: proportional would be 140px out), draggable+persisted divider, outline rail, image paste/drop/pick into the document's `assets/`, lazy syntax highlighting (53.7 kB gz, only when a fence exists). First brief in the run whose problem list was accurate — but relative image links had **never rendered**, and preview links navigated the whole desktop away |
| 61 | Code Editor | **SUPERSEDED by 88** (user asked directly for file creation + VS Code features) |
| 62 | Docs | **DONE 2026-08-03** — [brief](../briefs/done/62-docs-save-safety.md). Found + fixed: Docs could open **no** `.docx` in any shipped image (CSP refuses fflate's `blob:` worker; the promise never settled). Also killed SuperDoc's telemetry beacon |
| 63 | Sheets | **DONE 2026-08-03** — [brief](../briefs/done/63-sheets-fidelity-and-failure.md). Fidelity matrix is now a test. Found + fixed: **no workbook with a chart would open**, comments broke for non-Excel writers, and a merged range was written back as N copies of its value. Plus CSV in/out |
| 64 | Slides | **DONE 2026-08-03** — [brief](../briefs/done/64-slides-viewer-depth.md). Stays a viewer (locked). Thumbnail rail, keyboard nav, presenter mode, zoom, speaker notes, PNG export |
| 65 | PDF (both) | **DONE 2026-08-03** — [brief](../briefs/done/65-pdf-two-apps-one-extension.md). `pdf` → norPDF; PDF Viewer kept as the light option (retiring it buys 5 KB — pdf.js is shared). Also fixed: norPDF's Open read the **host** filesystem |
| 66 | norPDF | **DONE 2026-08-03** — [brief](../briefs/done/66-norpdf-document-and-harden.md) + [wiki page](norpdf.md). Write path proven to *preserve* (12 tests). Found + fixed: **every save in the OS could truncate the user's file** — `copyFile` onto the destination, now staged + renamed |
| 67 | Image Viewer | **DONE 2026-08-04** — [brief](../briefs/done/67-image-viewer-navigation-and-edit.md). Pan + rotate that persists. The brief's EXIF item was **measured wrong** (the browser already orients; applying it would double-rotate). Found + fixed: **"Fit to window" had never fit anything, in three apps** — now core's [`useElementSize`](../../apps/core/src/shared/hooks/useElementSize.ts) |
| 68 | Media Player | **DONE 2026-08-05** — [brief](../briefs/done/68-media-player-playlist-and-codecs.md). Volume/mute/rate/repeat/shuffle persist, per-file resume with thresholds, shuffle+repeat on a derived order, durations in the queue, sidecar `.srt`/`.vtt` via `addTextTrack` (a `<track>` cannot work: octet-stream MIME, and `blob:` is refused by the CSP's missing `media-src`), fullscreen + volume keys. The codec message was already half-there; the Open dialog offering formats the app **rejects** was not |
| 69 | Snipping Tool | **DONE 2026-08-05** — [brief](../briefs/done/69-snipping-tool-honest-capture.md). Opens to a launcher (no more screen hijack), Escape returns to it, delay capture with a click-through countdown, opaque **Black out** beside a much coarser pixelate, per-region canvas/video detection with an honest banner, reopen a saved capture. `getDisplayMedia` **rejected** (wrong surface, prompt per capture). Measured: the Terminal's text IS in a DOM capture — row ink profiles correlate 0.986 with a real screenshot |
| 70 | Calculator | **DONE 2026-08-05** — [brief](../briefs/done/70-calculator-reach-and-depth.md). Scientific tab on the **same** no-`eval` evaluator (parens, trig, logs, `^` right-assoc, `!`, π/e, deg/rad), memory keys + tape, measured `minSize` 300×430 with every key reachable at 1280×577. The float-noise item was **already fixed** — but only the rounded string was kept, so `1÷3 ×3` gave `0.999999999999`; the exact value now rides beside the display text. 79 tests in a package that had none |
| 71 | Clock | **DONE 2026-08-05** — [brief](../briefs/done/71-clock-timer-off-by-one-and-scheduling.md). First brief in the run whose problem list was accurate in **every** item. `ceil` for the countdown (measured: 6s timer now gives each second a full second, `00:00` only when it fires), and world clocks + alarms moved into the container behind a new authed `clock` module — the storage decision brief 72 shares: **a typed table per domain, not a new mechanism**, so DTOs can reject `25:00` at the door. One-time `localStorage` import that refuses a non-empty table. Weekday repeat, Snooze (in-window, because a toast has no buttons), multiple named timers with the timestamp model kept pure. Found: Start flashed `00:08` on a 6s timer from a stale `useNow` |
| 72 | Calendar | **DONE 2026-08-05** — [brief](../briefs/done/72-calendar-storage-and-recurrence.md). Events into the container using **brief 71's shape, not a new mechanism** (fifth app). Recurrence as a rule expanded per visible range — no RRULE library, `dayjs` stays the only date dep — with the three edit scopes as pure plans: "all" applies a **delta** so earlier occurrences survive, "this event" detaches, "this and following" splits and divides a `count`. ICS in/out through the real FS; an unrepresentable RRULE is refused and reported, never approximated. Agenda view + search + colours. The brief's **"no all-day events" was wrong** (it shipped in Wave C) — the real gap was multi-day *rendering*: a 4-day event showed in 1 cell, now 4. Its **1280×577 clip was already fixed by brief 52**, but `minSize` was dishonest anyway: at 480×380 a week row is **25px**, the date and nothing else → now 520×400 |
| 73 | Todo | **DONE 2026-08-05** — [brief](../briefs/done/73-todo-dates-and-structure.md). Due dates (**stored as the deadline instant**, so a bare date is 23:59:59.999 and `isOverdue` is a plain comparison — correct across midnight with no special case), priority, one level of lists, bulk actions, keyboard-first adding. The brief's "no ordering" was **half wrong** — drag-to-reorder already worked — but underneath it `reorder` stamped 1..N onto whatever ids it got, so reordering a **filtered** view overwrote the hidden rows' positions and two todos shared one. Also: `position` shipped `DEFAULT 0` so every legacy row tied → backfill. `minSize` 300×340 (chrome measured at 130px). Found: "Clear completed" was a **dead button on the Active tab**; write failures notified nowhere. Handed to 75: `PRAGMA foreign_keys` is off, so bookmark_links' CASCADE is decorative and deleting a group **orphans its links** |
| 74 | Sticky Notes | **DONE 2026-08-05** — [brief](../briefs/done/74-sticky-notes-be-sticky-and-on-style.md). Both halves: the style debt paid (rows are real `<button>`s with the controls as **siblings**, `ScrollArea`, ghost `Button`s, one `reportFailure()` on every mutation — the last console-only failure path in any add-on) and notes now live **on the desktop** behind a new core seam, `desktopLayer` on **`AppConfig`** (not `AddonManifest`: the shell reads `AppConfig[]`, so the manifest type typechecks in the add-on and fails in core). Core still imports no add-on. The layer sits **above** the icon grid (which spans the whole desktop and would otherwise eat every click) and **below** every window. **The colour question the brief asked to be grilled was already settled by brief 72** — its six-name palette as tinted border + low-alpha fill, reused not reinvented; saturated sticky yellow rejected as off-identity and surface-container greys as five indistinguishable greys. The brief's suspected delete-vs-open bug was **not real** (`stopPropagation` was already there) and is now moot: siblings, not nesting. `pos_x`/`pos_y` **reused** as the desktop position, so the migration moves no data and existing notes default to list-only. Drag/resize persist **once on release** via `setPointerCapture`, no new dependency; the clamp is a pure tested module because a note dropped off the edge cannot be recovered. Found: the layer mounts the query at **page load**, so a stale cache is reachable for the first time — verified it 404s gracefully, notifies, and self-corrects |
| 75 | Bookmarks | **DONE 2026-08-05** — [brief](../briefs/done/75-bookmarks-model-for-the-browser.md). Nested folders via `parentId` with a cycle guard (walks **up** from the proposed parent — a cycle exists exactly when the moved folder is on that chain), `href`→`url` renamed to the SQLite column as the contract brief 50 consumes, Netscape-HTML import/export hand-rolled (the format is not real HTML, and `DOMParser` is untestable in the `node` vitest every add-on uses), path-preserving search, duplicate detection on a normalised URL with the normalisation choices written down. **Fixed the bug 73 handed over**: `deleteGroup` trusted an `ON DELETE CASCADE` that is decorative because `PRAGMA foreign_keys` is off, so every folder deletion orphaned its links invisibly — now an explicit subtree delete in one transaction, plus a migrate-time sweep that deletes orphaned links (the user confirmed losing them) but promotes a stranded folder to the root (nobody did). Found: **`@IsUrl()` was wrong in both directions** — it rejected `http://localhost:3000` and the machine's own hostname while accepting `ftp:`, so replaced by an `http(s)`-only allow-list, which import made security-relevant (`javascript:` in an untrusted file → stored XSS via `<a href>`). Found: **core's `Select` rendered the raw `value`, not the label**, affecting git-gui and Calendar too — fixed once in core. Found: my own delete confirm called a full folder "empty" |
| 76 | Git GUI | **DONE 2026-08-05** — [brief](../briefs/done/76-git-gui-branches-and-hunks.md). Allowlist 7 → 12 subcommands (branch/switch/restore/stash/amend/apply/for-each-ref/symbolic-ref) with the hardened seam **unchanged in shape**: one `execa` call, array args, no shell, `--` guard, jail. The crux: **a ref is not a pathspec and `--` cannot protect it**, so `assertRefName` refuses `-D` / `--upload-pack=…` / `a..b` **before they become arguments** (27 hostile names tested); the pathspec guard is not reused because it permits `-` and `..` on purpose. Per-hunk staging is `git apply --cached` with the patch on **stdin** (the seam's one new capability), safe because git's default path handling was **measured on 2.43** — `../outside` → "does not exist in index", `../../etc/x` → "invalid path" — so `--unsafe-paths` is never passed and a test asserts its absence. **Grilled two departures**, both recorded in decisions.md: no server-side dirty-switch block (git already refuses the unsafe case and allows the safe one; the warning belongs in the UI) and discard is tracked-files-only (untracked means `git clean`, a different verb). Push/pull/fetch explicitly **out**, with the credential reason written down. Found: the 10 MB cap **failed silently** — an overrun looked like "no changes" — and then my own frontend swallowed the fixed 413 by feeding it to the diff parser; both fixed and measured against a real 16.7 MB diff. Also a phantom context line that corrupted every rebuilt patch's `@@` counts, caught by a round-trip test |
| 77 | REST Client | **DONE 2026-08-05** — [brief](../briefs/done/77-rest-client-workflow.md). Environments with `{{var}}` interpolation applied **at send time and never stored**, so one saved request works against local and deployed; the guard the review targets is a value that smuggles a scheme (`base = file:///etc/passwd`) or a `\r\n` into a header — refused client-side **and** by the proxy, defence in depth where the shallower layer gives the better message. Bearer writes a `{{var}}` reference, never the token. curl in/out hand-written with 40 tests (`$(…)` stays literal — a pasted command is data, not a shell), import previewing what it parsed and naming what it dropped. Multipart + raw binary via a `bodyBase64` on the proxy DTO, which bypasses nothing (every guardrail is upstream of the body). **Item 2 was half wrong and hid a real bug**: history WAS surfaced with replay, but stored only method+url, so replaying a GET while a POST body and an Authorization header were loaded sent something else entirely, with the previous credentials. Found: the tokenizer mishandled `\'` so `toCurl → parseCurl` did not round-trip; my own Bearer helper defaulted to `{{base}}`; every `notify()` lacked `appId`. SSRF stance **unchanged** and re-verified |
| 78 | Archive Manager | **DONE 2026-08-05** — [brief](../briefs/done/78-archive-manager-formats-and-progress.md). `GET /archive/list` reads a zip's central directory or `tar -tv` and **extracts nothing** (proved by a before/after directory snapshot), so the app now opens to a browser with search, sizes and dates instead of a progress bar. Selective extraction treated as the new attack surface it is: each chosen name goes through the same `resolveEntry` jail, a name the archive **does not declare** is refused rather than handed to tar, **every** declared entry is still checked (else not-selecting the bad entry would be the bypass — a zip with one safe file and one `../` is refused even when only the safe one is picked), and tar members go after `--` so `-rf.txt` cannot be an option. A refused entry is **reported, not hidden** — a listing that dropped them would lie about the file — and becomes a banner naming them. Progress by polling with a **CSPRNG UUID** id (it is the only thing naming a result), TTL+capped, and a failed job carries the service's own sentence instead of ending in silence. **The format matrix was checked, not guessed** — Docker pulls are blocked, so busybox's source + `aports` config as in brief 68 — and it is asymmetric: read `.tar.xz` **yes** (`CONFIG_FEATURE_SEAMLESS_XZ=y`), create `.tar.xz` **no** (`# CONFIG_XZ is not set`; tar `execlp`s a separate compressor, `archival/tar.c:573-621`), so xz is extract-only. The item asking to "add `.tar`/`.tgz`" was **half wrong** — they were already there. Non-UTF8 names decoded lossily and the row flagged **repaired**, not CP437-guessed (a mis-guessed codepage is a *different* wrong name with no warning); encrypted zips detected and declined with the reason. Add-to-existing dropped as the brief allowed: both routes give up "a failed operation leaves the original untouched" |

## User-requested, 2026-08-02 (87-90)

| # | Brief | Size | Ask |
|---|---|---|---|
| 87 | [file-explorer-properties-and-new-file](../briefs/done/87-file-explorer-properties-and-new-file.md) | EASY/MED | **DONE 2026-08-02** — Properties, New File…, and the entry context menu made reachable at all |
| 88 | [code-editor-files-and-vscode-features](../briefs/done/88-code-editor-files-and-vscode-features.md) | MEDIUM | **DONE 2026-08-02** — File/Edit/View menus, New File + New Folder, Save As, no native confirm; Monaco workers confirmed live |
| 89 | [media-player-vlc-transport](../briefs/done/89-media-player-vlc-transport.md) | MEDIUM | **DONE 2026-08-02** — ±5s, scrub bar with buffered range, speed, hotkeys |
| 90 | [office-medium-parity](../briefs/done/90-office-medium-parity.md) | HARD | **DONE 2026-08-03** — the gap was not missing UI (both engines ship full ribbons) but that the xlsx bridge dropped underline, font, size, alignment, wrap and borders on save. Plus Docs find + word count |

## Found while verifying, not from a brief (91-92)

| # | Brief | Size | What |
|---|---|---|---|
| 91 | [pdf-blank-pages-map-getorinsert](../briefs/done/91-pdf-blank-pages-map-getorinsert.md) | SMALL fix, TOTAL breakage | **DONE 2026-08-03** — every PDF page rendered blank on Chromium 141; pdf.js 6.1 needs `Map.prototype.getOrInsertComputed` (Chrome 142+). Polyfilled |
| 92 | [workers-and-wasm-study](../briefs/done/92-workers-and-wasm-study.md) | STUDY | **DONE 2026-08-03** — wasm is refused outright by the shipped CSP; measured which workloads belong in workers. CSV is the one real candidate |

## Real-OS parity (79-86)

| # | Brief | Size | Why |
|---|---|---|---|
| 79 | [trash](../briefs/done/79-trash.md) | MEDIUM | **DONE 2026-07-31** — freedesktop-spec Trash, restore, adversarially tested |
| 80 | [backup-and-restore](../briefs/done/80-backup-and-restore.md) | MEDIUM | **DONE 2026-08-06** — Settings → Backup replaces the README's `docker run`, which two of the product's own audiences (kiosk ISO, hosted instance) cannot run at all. The archive **streams** out of tar to the response — writing it into the tree being archived is both a recursion trap and a disk trap on a near-full volume — and the database rides as a `VACUUM INTO` snapshot with the live WAL trio excluded, because a hot tar of `db.sqlite` is torn *and* silently drops the `-wal`. **Two more format facts checked in busybox's source, and both changed the design**: `-C` is single-valued (`xchdir` once, after parsing), so GNU's multi-`-C` idiom would have produced a differently-rooted archive on Alpine — the manifest and snapshot live inside the tarred tree instead; and `--exclude` is unanchored at every `/` boundary, so a bare `.imbatranim/db.sqlite` would also have eaten a user's own `Documents/.imbatranim/db.sqlite` — every pattern is `./`-prefixed, which anchors it because member names keep their `./` (`strip_unsafe_prefix` drops `/` and `../`, not `./`), with a test that plants that file and asserts it survives. Restore reuses the hardened extractor by **splitting** it — `extractTar` → `stageTarExtraction()` + `mergeTree()` — so there is no second copy of the traversal check to rot; the one bound that moves is the 512 MB zip-bomb cap, replaced for restore by actual free disk, with every security guarantee unchanged. The swap is per top-level entry with **one undo entry per completed rename, not per name** — the distinction that makes a failure between parking the old entry and moving the new one recoverable. Refuses before staging: no manifest, wrong product, unreadable date, or a missing snapshot in the staged tree (which would otherwise boot the OS into setup with the data present but unreachable). Free space is an exact figure from `tar -tzv` via brief 78's `parseTarListLine`, not a guess about gzip. `RESTORE` typed and `@Equals`-checked **server-side**; sessions revoked and the cookie cleared, because the restored database carries the backup's password. **No password re-prompt on the download** — it grants nothing the session lacks, since `db.sqlite` is already readable through `/api/files`. Progress is the browser's own: a `fetch` counter would hold the whole volume in the tab's heap |
| 81 | [default-apps-and-open-with](../briefs/done/81-default-apps-and-open-with.md) | MEDIUM | **DONE 2026-08-06** — **The dead double-click is gone.** Apps declare `opens?: string[]` in their own manifest and core derives ext → candidates from `APP_REGISTRY`, so the 100-line map in the file manager is deleted and adding an app no longer means editing another app. Resolution is **user override → declared candidate → text fallback → nothing**, and `resolveOpener` returns *why*, which is what lets Files show a chooser only in the genuine "we don't know" case: text-ish files (derived from what the text apps claim, not a hand list) open in Code Editor, and an unknown binary opens the **Open with** chooser saying "No app claims this file type" — the brief asked for a Properties card first, which is one extra click to reach the only action it offers. New Settings → **Default apps**, rows computed from the registry so a new app appears with no second edit. **Two regressions the brief's own "must preserve" list caught**: `.md` dead-ended when Markdown Editor was disabled (the first cut hand-listed text extensions and missed `.md` — reintroducing the exact click this brief removes), and `.pdf` would have silently reverted brief 65 because both PDF apps claim it and `pdf-viewer` is registered first. **The bug only a second browser could see**: the override was wired to brief 49's `prefsStorage` and passed every same-tab check including a reload, but `writePref` drops any key absent from `DOTFILE_KEYS` — so it never left localStorage, and `rehydrateDotfileStores` was missing the store too. Registering a store in `DOTFILE_KEYS` is load-bearing, not bookkeeping; pinned by a unit test and a two-profile probe. Archives are the one **new** mapping and nearly shipped broken: Archive Manager drains a typed intent and ignored the generic `{ openPath, root }` payload, so the double-click launched it *idle and empty* — worse than a dead click. Fixed in the app that owns the knowledge, mapping "open" to brief 78's list-and-wait browse. **`opens` is a promise the app can act on the generic payload**, and reading its intent handler is the check. Not built as brief 48's `system.intents` (48 has not landed); shaped as free functions so it can move without changing callers — recorded debt |
| 82 | [startup-apps](../briefs/todo/82-startup-apps.md) | EASY/MED | Brief 49 removes layout restore and leaves nothing in its place |
| 83 | [storage-and-disk-full](../briefs/done/83-storage-and-disk-full.md) | MEDIUM | **DONE 2026-07-31** — bounded size walk, ENOSPC → 503, Settings → Storage |
| 84 | [system-log-and-audit](../briefs/done/84-system-log-and-audit.md) | MEDIUM | **DONE 2026-08-06** — The machine can finally answer "was anyone trying to log in as me last week?". **The brief's own argument overruled its own proposal**: it asks for a Nest logger *transport*, two paragraphs after saying "an audit trail assembled from incidental log lines is not a trail" — a transport would pour every `RouterExplorer` line into the file and push real events out of the rotation window, so Nest's stdout logging is untouched and `record()` is called on purpose at each site (sign-in ok/failed, lockout per-IP and global, password change/refusal, TOTP on/off, claim, kill, permanent delete, Trash emptied, backup taken/restored). Backend errors arrive via an exception filter recording **only 5xx** — a 404 is the system working — that extends `BaseExceptionFilter` and changes no response. **Never logging a secret is enforced, not promised**: redaction lives in `record()`, not at the call sites (a rule each caller must remember is a leak waiting for the one who forgets), denies by key name including **`hash`** (an argon2 hash is the input to offline cracking, and a log is easier to leak than a database), and the failed-login site never hands the DTO over at all. Verified on the real server: three refused sign-ins recorded, neither the attempted nor the real password anywhere in the file. Writes are **fire-and-forget** so a full disk cannot fail the request that caused it — a login must not break because the audit log cannot be written. Reads walk **backwards in 64 KB chunks**, filter the raw line before parsing, and stop at `limit`; a size cap is pointless if reading it needs the whole file in the heap. A torn line is skipped rather than poisoning the rest. Both routes authed, because log content names the addresses that tried to get in — an open read would be reconnaissance for the exact attacker it exists to catch. Brief 47's boundary makes the **browser a writer**, handled as such: appId + 300 chars and nothing else (a client-controlled object in a log file is log injection with no upside), a per-process budget, and `source: 'client'` so it can never pass for a server observation — a request forging `source: 'server'` had it dropped by the whitelisting pipe. Two dependency bugs the tools caught: `@Global` was **not enough** (the e2e suites build partial graphs, so every one failed to boot — required consumers now import `LogsModule` explicitly), and core's eslint refused `RecentSignIns` importing the add-on, so the log's shape moved into core as the backend contract it is. Ships the **System Log** app (virtualized, server-side filters, Follow) and **Settings → Security → Recent sign-ins**, first in that section, with refusals shown beside successes |
| 85 | [workspaces](../briefs/done/85-workspaces.md) | MEDIUM | **DONE 2026-08-06** — Four virtual desktops: `workspaceId` on the instance, pips beside the Tray with an occupancy dot, `Ctrl+Alt+←/→` wrapping both ways, and taskbar right-click → Move to workspace (which **follows**). **Switching hides windows, it does not unmount them.** Filtering the container list looks identical and is badly wrong — React would tear down the Terminal's PTY, discard an editor's unsaved buffer and restart every in-flight request once per switch. A window on an inactive workspace uses the same `display:none` a *minimised* one already did; proved by typing a digit into the Calculator, switching away and back, and finding it still there. **The brief was wrong about persistence, and wrong invisibly**: it calls this session state, but the window layout is *already* persisted and restored on boot, so omitting `workspaceId` would silently collapse four workspaces onto one on every reload. Persisted, along with the active workspace — landing on an empty desktop after a reload reads as "everything is gone". `clampWorkspace` is the enforcement point for the hard invariant that **no window can become unreachable**: a hand-edited `workspaceId: 47` lands on 4, not nowhere. `focusWindow` now follows the window to its workspace — one change at the place the taskbar, Alt+Tab, `openApp` and notification clicks all funnel through, because otherwise clicking a toast from another desktop raises a window you cannot see. **Two pre-existing bugs surfaced by the probe**: `ctrl+…` bindings had **never fired off a mac** (the matcher computes `mod` as ctrl there, then rejected any explicit `ctrl+` binding using the very key it asked for — invisible because every prior binding used `mod`), and **desktop icons and the Start menu both bypassed the single-instance rule**, each having grown its own `openWindow` call — untidy before, but with workspaces the duplicate opens on another desktop while the original looks lost. All four window hotkeys scoped, not just Alt+Tab: the same selector drives `Ctrl+W`, which could otherwise close a window on a desktop you cannot see |
| 86 | [keyboard-shortcut-registry](../briefs/done/86-keyboard-shortcut-registry.md) | EASY | **DONE 2026-07-31** — registry + `?`/F1 overlay + Settings section |

## Dependency order

1. **86** (shortcut registry) before anything that adds a hotkey — so new
   bindings register rather than stay invisible.
2. **52 → 53** before **85**, so `windowStore` and the desktop layer are not
   rewritten twice.
3. **54** before the app briefs that consume the picker (59-69, 76-78).
4. **79 (Trash) → 83 (Storage) → 80 (Backup)**: Backup excludes the Trash,
   Storage reports it.
5. **49 (durable dotfiles)** gates 81, 82, and the storage move in 71/72 — they
   all belong in the one prefs table; do not add a second config store.
6. **48 (the `system` seam)** gates 81, which should be `system.intents`' first
   real payload.
7. **65** (which PDF app wins) before **66**, and **75** before **50**.

## What is deliberately not here

Rejections are recorded in [real-os-gaps.md](real-os-gaps.md) so they are not
re-litigated: anything needing root, a runtime package manager, a services
daemon, a global audio mixer, a man-page reader, printing via CUPS, a clipboard
manager, a screen recorder, and an SSH client app.
